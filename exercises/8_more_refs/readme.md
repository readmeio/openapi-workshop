Let’s do one more `"``$refs``"` just for good measure! (We'll use it later)

**Instruction: Write a** `**schema**` **called** `**"**``**Squawk**``**"**` **that is identical to** `**"**``**Hoot**``**"**` ****